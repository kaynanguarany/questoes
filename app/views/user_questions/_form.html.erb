<%= form_for [current_user, @user_question] do |f| %>
  <% if @user_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_question.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% @user_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_field :subject %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
