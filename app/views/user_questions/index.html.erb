<p id="notice"><%= notice %></p>

<h1>Questões de <%= current_user.email %></h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Subject</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_questions.each do |user_question| %>
      <tr>
        <td><%= user_question.title %></td>
        <td><%= user_question.subject %></td>
        <td><%= link_to 'Show', user_question_path(current_user, user_question) %></td>
        <td><%= link_to 'Edit', edit_user_question_path(current_user, user_question) %></td>
        <td><%= link_to 'Destroy', user_question_path(current_user, user_question), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Question', new_user_question_path %>
