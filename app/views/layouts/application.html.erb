<!DOCTYPE html>
<html>
  <%= render "layouts/header"  %>
<body>

<%= render "layouts/nav"  %>

<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id => "flash_#{name}" %>
<% end %>

<%= yield %>

</body>
</html>
